class GoogleSheetConnector:

    def save_result(
        self,
        lead,
        result
    ):

        print(
            f"Saving Lead: {lead}"
        )

        print(
            f"Result: {result}"
        )
